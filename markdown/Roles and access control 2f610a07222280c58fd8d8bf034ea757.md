# Roles and access control

| Role | Key actions |
| --- | --- |
| **User** | Send borrow request (only Users can request), search available items, view own borrow records |
| **Operator** | Approve requests, assign items, handle pickup/return, view borrow history, edit borrowing records (e.g., In-use → Available) |
| **Admin** | All Operator actions, plus modify item master data and manage inventory records |